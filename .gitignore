node_modules/
.next/
dist/
build/
# local file storage — transfer to the server separately, never in git
server/_storage/
# secrets
.env
.env.local
**/.env
**/.env.local
# DB dump contains client PII + password hashes — deploy to server, NOT to GitHub
data-dump.sql
*.local
.DS_Store
.playwright-mcp/
