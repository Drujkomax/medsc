# Med Service Centre — web (Next.js)

Public site + admin UI. The Next app is at the **repo root** → deploys to Vercel directly (no root-directory config).

- Env: `NEXT_PUBLIC_API_URL=https://medsc.api.jaragency.uz`, `NEXT_PUBLIC_SITE_URL=https://medsc.uz`
- API/server is deployed separately on Contabo (`medsc.api.jaragency.uz`).
- Previous Lovable/Vite/Supabase app is on the **`old`** branch.

## Dev
```bash
npm i && npm run dev    # http://localhost:6002
```
